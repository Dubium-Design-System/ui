/**
 * Свойства компонента TableHead.
 *
 * @remarks
 * Заглушка для будущей реализации заголовка таблицы.
 */
interface TableHeadProps {}

/**
 * Компонент заголовка таблицы.
 *
 * @remarks
 * В настоящее время находится в разработке. Представляет собой заглушку
 * для будущей реализации компонента заголовка таблицы.
 *
 * @param props - Свойства компонента.
 * @returns Пустой React-элемент.
 */
export const TableHead = ({}: TableHeadProps) => {
	return <></>
}
