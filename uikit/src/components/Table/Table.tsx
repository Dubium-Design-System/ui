/**
 * Свойства компонента Table.
 *
 * @remarks
 * Заглушка для будущей реализации компонента таблицы.
 */
interface TableProps {}

/**
 * Компонент таблицы.
 *
 * @remarks
 * В настоящее время находится в разработке. Представляет собой заглушку
 * для будущей реализации полноценного компонента таблицы.
 *
 * @param props - Свойства компонента.
 * @returns Пустой React-элемент.
 */
export const Table = ({}: TableProps) => {
	return <></>
}
