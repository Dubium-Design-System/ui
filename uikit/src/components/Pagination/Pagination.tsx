/**
 * Свойства компонента Pagination.
 *
 * @remarks
 * Заглушка для будущей реализации компонента пагинации.
 */
export interface IPaginationProps {}

/**
 * Компонент пагинации.
 *
 * @remarks
 * В настоящее время находится в разработке. Представляет собой заглушку
 * для будущей реализации полноценного компонента пагинации.
 *
 * @param props - Свойства компонента.
 * @returns Пустой React-элемент.
 */
export const Pagination = ({}: IPaginationProps) => {
	return <></>
}
